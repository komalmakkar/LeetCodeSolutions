class Solution {
    public int singleNumber(int[] nums) {
        int XOR = nums[0];
        for(int i = 1; i < nums.length; i++){
            XOR = XOR ^ nums[i];
        }
        return XOR;
    }
}
